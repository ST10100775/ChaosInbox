OPTIONAL GMAIL MODE
===================
Put the OAuth Desktop App JSON downloaded from Google Cloud here and rename it:

credentials.json

Never commit credentials.json to Git.
The app only requests the Gmail read-only scope.
